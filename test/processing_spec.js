describe('ProcessingJS', () => {

    describe('draw', () => {
        it('should stop calling draw on an exception', () => {

        });

        it('should start calling draw after clearing the exception', () => {

        });

        it('should stop calling draw on a lint error', () => {

        });

        it('should start calling draw after fixing a lint error', () => {

        });
    });

    describe('style globals', () =>{
        it('should reset state before running updated code', () => {

        });

        it('should not use the reset state in draw and event handlers', () => {

        });

        it('should use the reset state if the state is different at the end of the main program body', () => {

        });
    });
});
